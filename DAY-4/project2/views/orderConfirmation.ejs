<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <title>주문 완료</title>
    <link rel="stylesheet" href="/orderConfirmation.css" />
  </head>
  <body>
    <div class="container">
      <h1>주문이 완료되었습니다!</h1>
      <p class="order-number">주문 번호: <%= order._id %></p>

      <h2>주문 상세 정보</h2>
      <ul class="order-details">
        <% order.products.forEach(item => { %>
        <li class="order-item">
          <strong>제품:</strong> <%= item.name %> <br />
          <strong>수량:</strong> <%= item.quantity %> <br />
          <strong>가격:</strong> $<%= (item.price * item.quantity) %>
          <br />
        </li>
        <% }) %>
      </ul>

      <h3>배송 정보</h3>
      <div class="shipping-info">
        <p><strong>주소:</strong> <%= order.shippingInfo.address %></p>
        <p><strong>도시:</strong> <%= order.shippingInfo.city %></p>
        <p><strong>우편번호:</strong> <%= order.shippingInfo.postalCode %></p>
        <p><strong>국가:</strong> <%= order.shippingInfo.country %></p>
        <p><strong>주문 상태:</strong> <%= order.orderStatus %></p>
      </div>

      <div class="navigation">
        <a href="/products" class="button">제품 목록으로 돌아가기</a>
        <a href="/logout" class="button">로그아웃</a>
      </div>
    </div>
  </body>
</html>
